[tool.poetry]
authors = ["Your Name <you@example.com>"]
description = ""
name = "example_local_python_module"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core>=1.0.0"]
